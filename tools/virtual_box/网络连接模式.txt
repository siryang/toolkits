bridged(桥接模式)、NAT(网络地址转换模式)和host-only(主机模式)

bridged：相当于为局域网内增加一个主机，虚拟系统和主机平等存在于局域网。
host_only：虚拟系统和主机可以通信，(同一主机上)虚拟系统和虚拟系统可以通信。
nat：让虚拟系统借助NAT功能，通过宿主机器访问互联网，但是虚拟系统不能和其它机器通信，采用nat最大的好处是连接互联网方便。
内部网络: 虚拟机和局域网断开，只提供虚拟机和虚拟机之间的连接。
通用驱动：